
// This service worker file is intentionally left almost empty.
// Firebase's SDK will handle the logic for displaying notifications
// when it's loaded in the browser.

// The SDK looks for a file with this specific name, so do not rename or delete it.

// You can add custom service worker logic here if needed in the future,
// but for basic push notifications, it's not required.
self.addEventListener('push', (event) => {
  // Optional: You can add custom logic here if you want to handle the push event manually.
  // For most cases, Firebase handles this automatically.
});
