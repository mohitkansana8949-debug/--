
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Entire Firestore full open access (including real-time useCollection)
    match /{path=**} {
      allow read, write, list, get: if true;
    }
  }
}
