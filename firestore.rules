
rules_version = '2';

// Force redeploy to fix persistent permission issues.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
